package com.computedlogic.label

/** Horizontal text alignment within the label content area. */
enum class HAlign { LEFT, CENTER, RIGHT }

/** Vertical text alignment within the label content area. */
enum class VAlign { TOP, CENTER, BOTTOM }

